# Backend base URL. Deployed default; override locally to http://localhost:3000
NEXT_PUBLIC_API_BASE=http://localhost:3000/
# Cookie name for the httpOnly JWT set by /api/auth/login
AUTH_COOKIE_NAME=anubhav_jwt
# Public site origin — used for metadataBase, sitemap.xml and robots.txt
NEXT_PUBLIC_SITE_URL=https://anubhav2026.in
